# Backend API for the legal AI chat (law-back1). Required for /lawAi/chat to answer.
# Local: run the law backend on port 8787, then use:
VITE_API_BASE_URL=http://localhost:8787/api

# Production default (if unset): https://law-back1.vercel.app/api
# VITE_DEFAULT_LANG=ru
